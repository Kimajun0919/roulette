# API integration scaffold

- `client.ts`: fetch wrapper + draw endpoints (`/api/draws`)
- `types.ts`: draw request/response 모델

## Env
- `VITE_API_BASE_URL`: 백엔드 베이스 URL (예: `https://api.example.com`)

## Note
현재는 연결 준비 단계이며, 실제 엔드포인트 스펙에 맞춰 필드/경로를 조정해야 합니다.
